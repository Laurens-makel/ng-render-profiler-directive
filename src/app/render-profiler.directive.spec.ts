import { RenderProfilerDirective } from './render-profiler.directive';

describe('RenderProfilerDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderProfilerDirective();
    expect(directive).toBeTruthy();
  });
});
